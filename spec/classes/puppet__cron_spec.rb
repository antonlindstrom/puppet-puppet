require 'spec_helper'

describe 'puppet::cron', :type => :class do

  #describe 'cron' do
  #  let(:params) { { :ensure => 'present' } }
  #
  #  it do should contain_cron('manual-puppet').with(
  #    'ensure'     => 'present',
  #    'user'       => 'root',
  #    'command'    =>
  #    '/usr/sbin/puppetd --onetime --no-daemonize --logdest syslog > /dev/null 2>&1',
  #  ) end
  #end

end
